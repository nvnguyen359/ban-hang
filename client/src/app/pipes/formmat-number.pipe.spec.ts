import { FormmatNumberPipe } from './formmat-number.pipe';

describe('FormmatNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FormmatNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
