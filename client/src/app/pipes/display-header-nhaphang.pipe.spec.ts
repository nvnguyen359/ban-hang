import { DisplayHeaderNhaphangPipe } from './display-header-nhaphang.pipe';

describe('DisplayHeaderNhaphangPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayHeaderNhaphangPipe();
    expect(pipe).toBeTruthy();
  });
});
