import { ClassTodayPipe } from './class-today.pipe';

describe('ClassTodayPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassTodayPipe();
    expect(pipe).toBeTruthy();
  });
});
